on:
  pull_request:
    types: [ closed ]

jobs:
  clear-caches:
    runs-on: ubuntu-latest
    steps:
      - name: Clear caches
        uses: theAngularGuy/clear-cache-of-pull-request@v1.2.2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
